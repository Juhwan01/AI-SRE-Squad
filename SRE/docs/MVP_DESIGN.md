# War Room 2.0 MVP - 실제 서비스 설계

## 🎯 목표

Dynamic MCP 기반으로 **실제 동작하는** 컨테이너 풀 관리 시스템 구현

## 🏗️ 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                   War Room 2.0 Manager                       │
│                    (항상 실행)                                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Container Pool Orchestrator                         │   │
│  │  - 컨테이너 생명주기 관리                              │   │
│  │  - 자동 시작/종료                                      │   │
│  │  - 리소스 모니터링                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Tier-based Server Manager                           │   │
│  │  - Tier 1: Hot Pool (자주 사용)                       │   │
│  │  - Tier 2: Warm Pool (가끔 사용)                      │   │
│  │  - Tier 3: Cold Pool (필요시만)                       │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Usage Pattern Learner                               │   │
│  │  - 사용 통계 추적                                      │   │
│  │  - 자동 티어 조정                                      │   │
│  │  - 예측적 로딩                                         │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        ▼                   ▼                   ▼
┌──────────────┐   ┌──────────────┐   ┌──────────────┐
│ Docker MCP   │   │Postgres MCP  │   │  Redis MCP   │
│ Container    │   │ Container    │   │  Container   │
│              │   │              │   │              │
│ Status: 실행중│   │ Status: 대기 │   │ Status: 종료됨│
│ 마지막: 2분전 │   │              │   │              │
│ 종료: 28분 후 │   │              │   │              │
└──────────────┘   └──────────────┘   └──────────────┘
```

## 📦 핵심 컴포넌트

### 1. Container Pool Orchestrator

**역할:** Docker 컨테이너 생명주기 관리

**기능:**
- MCP 서버 컨테이너 시작/종료
- 상태 모니터링 (running, idle, stopped)
- 타임아웃 기반 자동 종료 (기본 30분)
- 리소스 압박 시 우선순위 기반 종료

**구현:**
```python
class ContainerPoolOrchestrator:
    async def start_container(self, mcp_server: str) -> Container
    async def stop_container(self, container_id: str)
    async def get_container_status(self, container_id: str) -> ContainerStatus
    async def cleanup_idle_containers(self)
```

### 2. Tier-based Server Manager

**역할:** 서버를 사용 패턴에 따라 계층화 관리

**티어 정의:**

**Tier 1 - Hot Pool:**
- 조건: 주간 사용 10회 이상
- 상태: 이미지 항상 최신, 빠른 시작 준비
- 예시: `docker-mcp`, `kubernetes-mcp`, `postgres-mcp`
- 시작 시간: ~3초

**Tier 2 - Warm Pool:**
- 조건: 주간 사용 3-9회
- 상태: 이미지 캐시, 필요시 시작
- 예시: `redis-mcp`, `mongodb-mcp`, `aws-mcp`
- 시작 시간: ~5초

**Tier 3 - Cold Pool:**
- 조건: 주간 사용 0-2회
- 상태: 필요시 NPM에서 다운로드
- 예시: 나머지 모든 MCP 서버
- 시작 시간: ~30초 (최초), ~10초 (재사용)

**자동 티어 조정:**
```python
# 매일 자정 실행
def adjust_tiers():
    stats = analyze_weekly_usage()
    for server in all_servers:
        if stats[server] >= 10:
            move_to_tier1(server)
        elif stats[server] >= 3:
            move_to_tier2(server)
        else:
            move_to_tier3(server)
```

### 3. MCP Container Lifecycle

**상태 다이어그램:**
```
[불필요] → [필요 감지] → [시작 요청] → [컨테이너 시작]
                                            ↓
                                       [실행 중]
                                            ↓
                           [30분 미사용] ← [대기 중]
                                            ↓
                                        [종료]
                                            ↓
                                       [이미지 유지]
```

**생명주기 정책:**

1. **시작 조건:**
   - 장애 발생 및 MCP 서버 필요 판단 시
   - 수동 시작 명령

2. **실행 유지:**
   - Active: 현재 작업 중
   - Idle: 30분간 대기 (재사용 대비)

3. **종료 조건:**
   - 30분간 미사용 (Idle timeout)
   - 메모리 80% 초과 (리소스 압박)
   - 수동 종료 명령

4. **정리:**
   - 컨테이너 종료
   - 이미지는 유지 (빠른 재시작)
   - 1주일 미사용 시 이미지 삭제

## 🔄 동작 시나리오

### 시나리오 1: Docker 장애 발생

```
09:00 - Docker 컨테이너 장애 감지
  ↓
War Room이 에러 로그 분석
  "Error: Cannot connect to Docker daemon"
  ↓
키워드 추출: ["docker", "container", "daemon"]
  ↓
MCP Catalog 검색
  → @modelcontextprotocol/server-docker 발견
  ↓
Tier 확인
  → Tier 1 (자주 사용)
  → 이미지 준비됨
  ↓
컨테이너 시작 (3초)
  ↓
Docker MCP 서버로 문제 해결
  ↓
작업 완료 → Idle 상태 전환
  ↓
09:35 - 30분 경과 → 자동 종료
```

### 시나리오 2: 새로운 서비스 장애 (Kafka)

```
14:00 - Kafka 브로커 장애 발생
  ↓
War Room 분석
  "Kafka broker connection failed"
  ↓
키워드: ["kafka", "broker"]
  ↓
MCP Catalog 검색
  → kafka-mcp 발견 (Tier 3, 처음 사용)
  ↓
NPM에서 다운로드 및 이미지 빌드 (30초)
  ↓
컨테이너 시작 (10초)
  ↓
사용 통계 업데이트
  → 일주일 후 사용 빈도에 따라 Tier 조정
```

### 시나리오 3: 동시 다발 장애

```
10:00 - Docker, Postgres, Redis 동시 장애
  ↓
War Room이 3개 문제 동시 분석
  ↓
필요한 MCP 서버:
  - docker-mcp (Tier 1)
  - postgres-mcp (Tier 1)
  - redis-mcp (Tier 2)
  ↓
병렬 컨테이너 시작 (동시에 3개)
  - docker-mcp: 3초
  - postgres-mcp: 3초
  - redis-mcp: 5초
  ↓
총 소요 시간: ~5초 (병렬)
  ↓
3개 서버 동시 작업
  ↓
각각 독립적으로 Idle → 종료
```

## 📊 리소스 관리

### 메모리 정책

**기준:**
- War Room Manager: 200MB (고정)
- MCP 서버 컨테이너: 평균 100MB/개
- 최대 동시 실행: 10개 (총 1.2GB)

**압박 시 정책:**
```python
if total_memory_usage > 80%:
    # 가장 오래 Idle 상태인 것부터 종료
    containers = sort_by_idle_time(running_containers)
    for container in containers:
        if container.status == "idle":
            stop_container(container)
            if total_memory_usage < 70%:
                break
```

### 디스크 관리

**이미지 저장소:**
```
/var/war-room/images/
├── tier1/
│   ├── docker-mcp:latest (50MB)
│   ├── kubernetes-mcp:latest (80MB)
│   └── postgres-mcp:latest (60MB)
├── tier2/
│   ├── redis-mcp:latest (40MB)
│   └── mongodb-mcp:latest (70MB)
└── tier3/
    └── (필요시 다운로드)

총 디스크: ~500MB (Tier 1+2 이미지)
```

**정리 정책:**
- Tier 3 이미지: 1주일 미사용 시 삭제
- Tier 2 이미지: 1개월 미사용 시 Tier 3로 강등 후 삭제
- Tier 1 이미지: 항상 유지

## 🔒 안전성

### 컨테이너 격리

각 MCP 서버는 완전히 독립된 컨테이너에서 실행:
```yaml
docker run \
  --name docker-mcp-${timestamp} \
  --network war-room-network \
  --memory 200M \
  --cpus 0.5 \
  --read-only \
  --security-opt no-new-privileges \
  mcp/docker-server:latest
```

### 장애 격리

- 한 MCP 서버 크래시 → 다른 서버 영향 없음
- War Room Manager 재시작 → 컨테이너 상태 복구
- 전체 시스템 재시작 → 메타데이터 기반 복구

## 📈 성능 목표

### 응답 시간

| 상황 | 기존 시스템 | War Room 2.0 | 개선 |
|------|------------|--------------|------|
| Tier 1 서버 시작 | 15분 (수동) | 3초 (자동) | **300배** |
| Tier 2 서버 시작 | 15분 (수동) | 5초 (자동) | **180배** |
| Tier 3 서버 시작 (최초) | 30분 (수동) | 30초 (자동) | **60배** |
| 동시 3개 장애 | 45분 (순차) | 5초 (병렬) | **540배** |

### 리소스 효율

**시나리오: 하루 중 Docker 장애 1회만 발생**

| 방식 | 메모리 사용 | 효율성 |
|------|------------|--------|
| Monolithic | 1.5GB (24시간) | 36GB·h |
| 모두 실행 | 15GB (24시간) | 360GB·h |
| **War Room 2.0** | **100MB (30분) + 200MB (23.5시간)** | **4.9GB·h** ✅ |

→ **73배 효율적**

## 🎯 MVP 구현 우선순위

### Phase 1: 핵심 기능 (1주)
- [ ] `ContainerPoolOrchestrator` 구현
- [ ] Docker SDK 통합
- [ ] 기본 생명주기 관리 (시작/종료)
- [ ] 간단한 타임아웃 로직

### Phase 2: 지능형 관리 (1주)
- [ ] `TierBasedServerManager` 구현
- [ ] 사용 통계 수집
- [ ] 자동 티어 조정
- [ ] 리소스 모니터링

### Phase 3: 운영 안정성 (1주)
- [ ] 장애 복구 로직
- [ ] 로깅 및 모니터링
- [ ] 성능 최적화
- [ ] 통합 테스트

## 🧪 테스트 계획

### 단위 테스트
- 컨테이너 시작/종료
- 티어 조정 로직
- 타임아웃 처리

### 통합 테스트
- End-to-end 장애 시나리오
- 동시 다발 장애
- 리소스 압박 상황

### 성능 테스트
- 컨테이너 시작 시간 측정
- 메모리 사용량 프로파일링
- 동시 처리 성능

## 📁 파일 구조

```
SRE/
├── src/
│   ├── container_orchestrator.py    # 컨테이너 풀 관리
│   ├── tier_manager.py               # 티어 기반 관리
│   ├── usage_tracker.py              # 사용 패턴 추적
│   ├── lifecycle_manager.py          # 생명주기 관리
│   └── war_room.py                   # (기존 확장)
├── docker/
│   ├── Dockerfile.manager            # War Room Manager
│   ├── Dockerfile.mcp-base           # MCP 서버 베이스
│   └── docker-compose.yml            # 전체 구성
├── config/
│   ├── tiers.json                    # 티어 설정
│   └── policies.json                 # 정책 설정
└── tests/
    ├── test_orchestrator.py
    ├── test_tier_manager.py
    └── test_integration.py
```

## 🚀 배포

### Docker Compose로 시작

```bash
# War Room 2.0 시작
docker-compose up -d

# 상태 확인
docker exec war-room-manager python -m src.war_room status

# 로그 확인
docker logs -f war-room-manager
```

### 클라우드 배포 (향후)

- AWS ECS / Fargate
- Kubernetes (HPA로 자동 스케일링)
- 모니터링: Prometheus + Grafana

## 💡 핵심 차별점

1. **Zero Configuration**: 사전 설정 불필요
2. **Just-in-Time**: 필요할 때만 리소스 사용
3. **Self-Learning**: 사용 패턴 학습 및 최적화
4. **Isolation**: 완벽한 컨테이너 격리
5. **Scalability**: 무한 확장 가능

---

**War Room 2.0**: 스스로 생각하고, 스스로 도구를 찾고, 스스로 최적화하는 AI SRE
